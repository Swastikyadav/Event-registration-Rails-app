<h1>Registrations for <%= link_to @event.name, event_path(@event) %></h1>
<ul>
  <% @registrations.each do |reg| %>
    <li>
      <%= reg.user.name %><br/>
      <%= reg.user.email %><br/>
      <%= reg.how_heard %>
    </li>
  <% end %>
</ul>